# Frontend

Vite 7 + React 19 + TypeScript + shadcn/ui

```bash
pnpm install   # install dependencies
pnpm dev       # start dev server
pnpm build:prod # production build
```

See [CLAUDE.md](./CLAUDE.md) for project structure, coding conventions, and detailed guidance.
